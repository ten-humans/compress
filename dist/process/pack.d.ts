import { packType, ScoreType } from "../interface/process";
export declare function ReadData(data: string): ScoreType;
export declare function process(data: string, map: object): string;
export default function pack(data: string): packType;
